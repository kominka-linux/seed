[package]
name = "seed"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = { path = "vendor/libc" }

[profile.dev]
debug = "line-tables-only"
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 2
