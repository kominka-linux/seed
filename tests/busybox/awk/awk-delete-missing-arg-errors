tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

if awk -e '{delete}' >"$tmpdir/actual" 2>"$tmpdir/error"; then
	exit 1
fi

grep -F 'too few arguments' "$tmpdir/error" >/dev/null
