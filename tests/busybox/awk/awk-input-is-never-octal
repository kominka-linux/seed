tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf '011\n' >"$tmpdir/input"

awk '{ print $1, $1+1 }' "$tmpdir/input" >"$tmpdir/actual"
printf '011 12\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
