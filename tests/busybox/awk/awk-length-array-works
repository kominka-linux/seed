tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

awk 'BEGIN{ A[1]=2; A["qwe"]="asd"; print length(A)}' >"$tmpdir/actual"
printf '2\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
