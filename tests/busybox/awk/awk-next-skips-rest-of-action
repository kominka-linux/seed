tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'left right\n' >"$tmpdir/input"

awk '{print $1; next; print "x"}' "$tmpdir/input" >"$tmpdir/actual"
printf 'left\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
