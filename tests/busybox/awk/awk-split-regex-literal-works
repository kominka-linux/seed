tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

awk 'BEGIN{print split("a1b2", a, /[0-9]/); print a[1]; print a[2]; print a[3]}' >"$tmpdir/actual"
printf '3\na\nb\n\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
