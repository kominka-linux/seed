tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

awk 'BEGIN{x="abc"; print sub(/b/,"&X",x), x}' >"$tmpdir/actual"
printf '1 abXc\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
