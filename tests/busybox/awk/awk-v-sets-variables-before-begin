tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

awk -v greeting=hello 'BEGIN { print greeting }' >"$tmpdir/actual"
printf 'hello\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
