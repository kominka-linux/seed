tmpdir="${TMPDIR:-/tmp}/seed-awk-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

awk 'BEGIN{i=0; while(i<3){print i; i++}}' >"$tmpdir/actual"
printf '0\n1\n2\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
