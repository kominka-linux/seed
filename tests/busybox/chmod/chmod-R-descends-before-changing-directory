mkdir parent
printf 'ok\n' > parent/file

chmod -R 644 parent

test "$(PATH=/usr/bin:/bin stat -c '%a' parent)" = "644"
chmod 755 parent

test "$(PATH=/usr/bin:/bin stat -c '%a' parent)" = "755"
test "$(PATH=/usr/bin:/bin stat -c '%a' parent/file)" = "644"
