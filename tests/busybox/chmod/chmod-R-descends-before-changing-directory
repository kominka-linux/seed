mkdir parent
printf 'ok\n' > parent/file

chmod -R 644 parent

test "$(stat -f '%Lp' parent)" = "644"
chmod 755 parent

test "$(stat -f '%Lp' parent)" = "755"
test "$(stat -f '%Lp' parent/file)" = "644"
