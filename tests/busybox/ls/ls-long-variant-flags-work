LC_ALL=C /bin/ls -ln "$d/a" "$d/big" > logfile.gnu
LC_ALL=C busybox ls -ln "$d/a" "$d/big" > logfile.bb
cmp logfile.gnu logfile.bb

LC_ALL=C /bin/ls -lg "$d/a" "$d/big" > logfile.gnu
LC_ALL=C busybox ls -lg "$d/a" "$d/big" > logfile.bb
cmp logfile.gnu logfile.bb

LC_ALL=C /bin/ls -lo "$d/a" "$d/big" > logfile.gnu
LC_ALL=C busybox ls -lo "$d/a" "$d/big" > logfile.bb
cmp logfile.gnu logfile.bb
