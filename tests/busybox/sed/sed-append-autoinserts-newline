tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'woot' >"$tmpdir/input"

sed -e '/woot/a woo' "$tmpdir/input" >"$tmpdir/actual"
printf 'woot\nwoo\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
