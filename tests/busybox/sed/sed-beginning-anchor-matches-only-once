tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf '/usr/lib\n' >"$tmpdir/input"

sed 's,\(^/\|\)[^/][^/]*,>\0<,g' "$tmpdir/input" >"$tmpdir/actual"
printf '>/usr</>lib<\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
