tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'first\nsecond\n' >"$tmpdir/input"

sed 'crepl' "$tmpdir/input" >"$tmpdir/actual"
printf 'repl\nrepl\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
