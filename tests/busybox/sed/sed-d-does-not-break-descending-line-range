tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'first\nsecond\nthird\nfourth\n' >"$tmpdir/input"

sed -n '2d;2,1p' "$tmpdir/input" >"$tmpdir/actual"
printf 'third\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
