tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'woo\0woo\0' >"$tmpdir/input"

sed -e 's/woo/bang/g' "$tmpdir/input" >"$tmpdir/actual"
printf 'bang\0bang\0' >"$tmpdir/expected"

cmp -s "$tmpdir/expected" "$tmpdir/actual"
