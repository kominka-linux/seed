tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf '\0woo\0woo\0' >"$tmpdir/input"

sed -e 's/woo/bang/' "$tmpdir/input" >"$tmpdir/actual"
printf '\0bang\0bang\0' >"$tmpdir/expected"

cmp -s "$tmpdir/expected" "$tmpdir/actual"
