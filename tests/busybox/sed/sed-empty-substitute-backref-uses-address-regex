tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'woot\n' >"$tmpdir/input"

sed -e '/woot/s//eep \0 eep/' "$tmpdir/input" >"$tmpdir/actual"
printf 'eep woot eep\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
