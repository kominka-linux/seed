tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'a' >"$tmpdir/input"

sed 's/a/z\
z/' "$tmpdir/input" >"$tmpdir/actual"
printf 'z\nz' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
