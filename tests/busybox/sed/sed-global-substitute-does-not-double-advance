tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf '_aaa1aa\n' >"$tmpdir/input"

sed 's/a/A/g' "$tmpdir/input" >"$tmpdir/actual"
printf '_AAA1AA\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
