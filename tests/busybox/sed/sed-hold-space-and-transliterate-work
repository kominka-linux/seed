tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf 'abc\nsecond\n' >"$tmpdir/input"

sed -n '
1y/abc/xyz/
1h
2g
p
' "$tmpdir/input" >"$tmpdir/actual"

printf 'xyz\nxyz\n' >"$tmpdir/expected"
diff -u "$tmpdir/expected" "$tmpdir/actual"
