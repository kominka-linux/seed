tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf '1\n2\n3\n4\n' >"$tmpdir/input"

sed '1,2d' -i "$tmpdir/input"
cat "$tmpdir/input" >"$tmpdir/actual"
printf '3\n4\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
