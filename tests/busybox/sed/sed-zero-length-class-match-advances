tmpdir="${TMPDIR:-/tmp}/seed-sed-$$"
trap 'rm -rf "$tmpdir"' EXIT
mkdir -p "$tmpdir"

printf ' a.b\n' >"$tmpdir/input"

sed 's [^ .]* x g' "$tmpdir/input" >"$tmpdir/actual"
printf 'x x.x\n' >"$tmpdir/expected"

diff -u "$tmpdir/expected" "$tmpdir/actual"
